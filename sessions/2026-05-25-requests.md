# 2026-05-25 — session requests

## 1. Prefix path above breadcrumbs
The parent-hint path can be arbitrarily long. Move it above the breadcrumb
links so breadcrumbs root at the left edge of the content pane, indented
slightly from the context path. Currently both sit in a flex row.

## 2. View text files
Open/view non-markdown text files (`.go`, `.txt`, `.toml`, etc.) in the
browser. Currently they show as unlinked `<span class="no-link">` in the
index and 404 when requested directly (`findFile` gates on `Class == "md"`).

## 3. Hidden/ignored/untracked status display
When toggled on to show hidden/ignored files, display their status more
explicitly — not just dimming. Consider deemphasized/desaturated background.
Show untracked (`??`) git status. The user notes it's a text viewer first,
so status decoration should be subtle.

## 4. Sidebar close button (Vimium Esc conflict)
Esc is bound by Vimium, so it can't close the sidebar. Add a clickable
close affordance. Principle: all features should be discoverable and
operable via mouse/click, not just keyboard.

## 5. Toggle inline diffs in file view
Add a toggle to show git diffs inline in the rendered file view.

## Prior uncommitted work
- Access-log feature (`--log-level off|dot|info`) — complete, tested, not committed
- Fuzzy matcher rewrite (fzf-style multi-start + merged highlights) — complete, tested
- sessions/2026-05-18-requests.md: URL prefix env var (`GLOW_WEB_URL_PREFIX`)
